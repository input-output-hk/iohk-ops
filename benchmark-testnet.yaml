args:
  configurationKey:
    tag: NixStr
    contents: bench
  EmurgoaccessKeyId:
    tag: NixStr
    contents: iohk
  systemStart:
    tag: NixInt
    contents: 1519741905
  CFaccessKeyId:
    tag: NixStr
    contents: iohk
  IOHKaccessKeyId:
    tag: NixStr
    contents: iohk
gen-cmdline: -v new -e benhmark -t topology-benchmark.yaml -k bench benchmarks110experiments
  nodes
environment: Benchmark
installer-bucket: default-bucket
topology: topology-benchmark.yaml
name: benchmarks110experiments
files:
- deployments/security-groups.nix
- deployments/cardano-nodes-env-staging.nix
- deployments/keypairs.nix
- deployments/cardano-nodes.nix
elements:
- Nodes
target: AWS
