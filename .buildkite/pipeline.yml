steps:
  - label: 'iohk-ops'
    command: 'scripts/ci.sh -b false'
    agents:
      - "system=x86_64-linux"
    agents:
      system: x86_64-linux
  - label: 'iohk-ops-shellcheck'
    command: "nix-build -A tests.shellcheck --no-out-link"
    agents:
      - "system=x86_64-linux"
    agents:
      system: x86_64-linux
